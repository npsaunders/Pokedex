<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('./partials/head.ejs')%>
</head>

<body>
  <div class="body-overlay">
    <!-- ============ HEADER ============ -->
    <%- include('./partials/header.ejs')%>
      <!-- ============ MAIN ============ -->
      <main>
        <div class="container">
          <h1>
            <%=pokemon.name%>
          </h1>
          <!-- BACK -->
          <a id="returnIndex" class="button button-primary" href="/pokemons">Back</a>
        </div><br />


        <img src=<%=pokemon.img%> alt="<%=pokemon.name%>" />
          <table>
            <tbody>
              <tr>
                <td>
                  types: <%=pokemon.type%>
                </td>
              </tr>
              <tr>
                <td>
                  hp: <%=stat.hp%>
                </td>
              </tr>
              <tr>
                <td>
                  attack: <%-stat.attack%>
                </td>
              </tr>
              <tr>
                <td>
                  defense: <%-stat.defense%>
                </td>
              </tr>
            </tbody>
          </table>
      </main>
  </div>
  </main>
  <!-- ============ FOOTER ============ -->
  <%- include('./partials/footer.ejs')%>
</body>

</html>